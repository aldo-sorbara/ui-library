---
name: Loading
menu: Components
route: /loading
---

import { Playground, PropsTable } from 'docz';
import Loading from '../../components/web/loading';
import { ThemeProvider } from 'styled-components';
import welp_ar from '../../branding/brands/welp_ar';
import GlobalStyle from '../../components/web/globalstyle';

# Emoji

<PropsTable of={Loading} />

## Basic usage

<Playground>
  <ThemeProvider theme={welp_ar.theme}>
    <>
      <GlobalStyle theme={welp_ar.theme} />
      <Loading
        loading
        text='Estamos calculando el mejor préstamo para vos.'
        style={{ position: 'unset' }}
      />
    </>
  </ThemeProvider>
</Playground>

[Try it on Storybook!](./storybook/index.html?path=/story/loading--with-default-props)
