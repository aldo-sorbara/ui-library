---
name: Grids
menu: Components
route: /grids
---

import { Playground, PropsTable } from 'docz';
import { Fragment } from 'react';
import { Grid, GridContainer } from '../../components/web/grids';
import { ThemeProvider } from 'styled-components';
import welp_ar from '../../branding/brands/welp_ar';
import GlobalStyle from '../../components/web/globalstyle';

# Grids

<PropsTable of={Grid} />
<PropsTable of={GridContainer} />

## Basic usage

<Playground>
  <ThemeProvider theme={welp_ar.theme}>
    <>
      <GlobalStyle theme={welp_ar.theme} />
      <Grid lg={25} md={25} sm={25} xs={25}>
        <p>This is a default Grid</p>
      </Grid>
      <GridContainer>
        <Grid lg={75} md={75} sm={75} xs={75}>
          <p>This is a default Grid with a container.</p>
        </Grid>
      </GridContainer>
    </>
  </ThemeProvider>
</Playground>

[Try it on Storybook!](./storybook/index.html?path=/story/grids--with-default-props)
